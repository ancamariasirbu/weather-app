# weather-app

A starting point for a weather application.

## Structure

- `server/` – backend API (Node/Express)
- `web/` – frontend (React)

## How to run (later)

Each folder will have its own `package.json` and start command.
